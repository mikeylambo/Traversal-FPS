# RC Playtest Notes

## 2026-09-21

- Campaign progression is fun and reaches at least Sector 33 in a real run.
- Sector 33 // ASCENT currently has two blocking traversal issues reported in playtest:
  - spawn is physically trapped inside the central vertical divider, so normal movement does not begin until a warp is performed;
  - the final target is occluded by the same divider, making the top platform/goal appear unreachable.
- Multiple campaign maps still feel structurally similar; continue using Level Lab similarity/triage after this hard-block fix rather than blindly rewriting the campaign.
- Map Editor remains inaccessible on touch-capable/desktop hybrid configurations because of coarse-pointer gating; RC tooling should be deterministic and keyboard-accessible.
